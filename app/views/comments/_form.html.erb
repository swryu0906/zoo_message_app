<h3>Post a Comment</h3>

<%= form_for [ @post, @comment ] do |f| %>
  <div class="">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="">
    <%= f.submit "Submit" %>
  </div>
<% end %>
